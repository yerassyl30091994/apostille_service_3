angular.module("app").service('$sound', [ '$http', '$q', '$rootScope', '$locales', '$injector',
    function ($http, $q, $rootScope, $locales, $injector) {

        $rootScope.$sound = this;//global service

/*        //Инициализация звуков
        jQuery.ionSound({
            sounds: [
                "ru01",					//Здравствуйте
                "ru02",					//Здесь вы можете...
                "ru03",					//Веберите услугу
                "ru04",					//Следуийте инструкции
                "ru05",					//Благаарим за пользование нашими услугами
                "ru06",					//Досвидание

                "kz01",
                "kz02",
                "kz03",
                "kz04",
                "kz05",
                "kz06"
                //Название аудио файла
                //Аудио файлы должны быть в двух форматах .mp3 и .ogg для кросплатформенности
            ],
            path: "kit/_sounds/", //Папка с аудио звуками
            multiPlay: true,			//Одновременное воспроизведение нескольких записей
            preload: true,				//Предзагрузка
            volume: "1.0"				//Громкость
        });*/


        var sounds = {
            welcome: "01",
            info: "02",
            selectService: "03",
            followInstructions: "04",
            blessment: "05",
            bye: "06"
        };


        var files = ['01', '02', '03', '04', '05', '06'];

        var enabled = false;
        this.enable = function(boolean){
            return enabled;
        };

        this.play = function (audio) {
/*            var $settings = $injector.get('$settings');
            if( $settings.isPep() || $settings.isMute() || enabled) return;

            angular.forEach(files, function (file) {
                jQuery.ionSound.stop(file);
            });

            jQuery.ionSound.play($locales.current() + sounds[audio]);*/
        };

        return this;
    }

]);